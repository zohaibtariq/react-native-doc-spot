/**
 * @format
 */

import {AppRegistry} from 'react-native';
// import App from './App';
import {name as appName} from './app.json';
import LightDarkMode from './LightDarkMode';

AppRegistry.registerComponent(appName, () => LightDarkMode);
